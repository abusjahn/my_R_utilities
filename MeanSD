MeanSD<-function(x,roundDig=2)
{
  out<-''
  meansd<-roundR(
    matrix(c(mean(x,na.rm=T),
             sd(x,na.rm=T)),
           ncol=2,byrow=F),level=roundDig)
  out<-paste(meansd[,1],meansd[,2],sep='\u00B1')
  #   }
  return(out)
}
